<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="me.ianhe.dao.SysAuthMapper">
    <resultMap id="BaseResultMap" type="me.ianhe.db.entity.SysAuth">
        <id column="id" property="id" jdbcType="INTEGER"/>
        <result column="auth" property="auth" jdbcType="VARCHAR"/>
    </resultMap>

    <resultMap id="AuthRoleResultMap" type="me.ianhe.db.entity.SysAuth">
        <id column="id" property="id" jdbcType="INTEGER"/>
        <result column="auth" property="auth" jdbcType="VARCHAR"/>
        <collection property="sysRoles" ofType="me.ianhe.db.entity.SysRole">
            <id column="id" property="id"/>
            <result column="name" property="name"/>
        </collection>
    </resultMap>

    <sql id="Base_Column_List">
        id, auth
    </sql>
    <select id="selectByPrimaryKey" resultMap="BaseResultMap" parameterType="java.lang.Integer">
        select
        <include refid="Base_Column_List"/>
        from sys_auth
        where id = #{id,jdbcType=INTEGER}
    </select>
    <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer">
        delete from sys_auth
        where id = #{id,jdbcType=INTEGER}
    </delete>
    <insert id="insert" parameterType="me.ianhe.db.entity.SysAuth" useGeneratedKeys="true" keyProperty="id">
        insert into sys_auth (auth)
        values (#{auth,jdbcType=VARCHAR})
    </insert>
    <insert id="insertSelective" parameterType="me.ianhe.db.entity.SysAuth" useGeneratedKeys="true" keyProperty="id">
        insert into sys_auth
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="auth != null">
                auth,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="auth != null">
                #{auth,jdbcType=VARCHAR},
            </if>
        </trim>
    </insert>
    <update id="updateByPrimaryKeySelective" parameterType="me.ianhe.db.entity.SysAuth">
        update sys_auth
        <set>
            <if test="auth != null">
                auth = #{auth,jdbcType=VARCHAR},
            </if>
        </set>
        where id = #{id,jdbcType=INTEGER}
    </update>
    <update id="updateByPrimaryKey" parameterType="me.ianhe.db.entity.SysAuth">
        update sys_auth
        set auth = #{auth,jdbcType=VARCHAR}
        where id = #{id,jdbcType=INTEGER}
    </update>

    <select id="selectSysAuth" resultMap="AuthRoleResultMap">
        select auth.*,role.*
        from sys_auth auth
        left join sys_auth_role ar on auth.id = ar.authID
        left join sys_role role on role.id = ar.roleID
    </select>
</mapper>