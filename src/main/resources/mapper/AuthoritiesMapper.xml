<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="me.ianhe.dao.AuthoritiesMapper">
    <resultMap id="BaseResultMap" type="me.ianhe.db.entity.Authorities">
        <result column="username" property="username" jdbcType="VARCHAR"/>
        <result column="authority" property="authority" jdbcType="VARCHAR"/>
    </resultMap>
    <sql id="Base_Column_List">
        username,authority
    </sql>
    <insert id="insert" parameterType="me.ianhe.db.entity.Authorities">
        insert into authorities (username, authority)
        values (#{username,jdbcType=VARCHAR}, #{authority,jdbcType=VARCHAR})
    </insert>
    <insert id="insertSelective" parameterType="me.ianhe.db.entity.Authorities">
        insert into authorities
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="username != null">
                username,
            </if>
            <if test="authority != null">
                authority,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="username != null">
                #{username,jdbcType=VARCHAR},
            </if>
            <if test="authority != null">
                #{authority,jdbcType=VARCHAR},
            </if>
        </trim>
    </insert>
    <select id="getUserAuthoritys" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from authorities
        where username = #{username,jdbcType=VARCHAR}
    </select>
</mapper>